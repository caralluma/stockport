import React from 'react';
import { Button } from 'react-toolbox/lib/button';
import theme from './add-button.css';

const AddButton = (props) => <Button {...props} theme={theme} />;
export default AddButton;
